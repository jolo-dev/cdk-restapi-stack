{
  "name": "lambdas",
  "version": "0.0.1",
  "description": "This is a package for Lambdas written in Typescript",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "esbuild ./src/**/**.ts --bundle --minify --platform=node --target=node14.7 --outdir=./build",
    "watch": "esbuild ./src/**/**.ts --bundle --minify --platform=node --target=node14.7 --outdir=./build --watch",
    "dev": "docker-compose up -d && ./init_db.sh",
    "lint": "eslint . --fix --ext .ts"
  },
  "author": "John Nguyen <john@globaldatanet.com>",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.43.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.85"
  }
}